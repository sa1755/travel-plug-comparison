import type { AdapterProduct } from "@/types";

/**
 * Add only reviewed, real retailer products here. Never commit invented prices,
 * product URLs, or affiliate URLs. An empty catalog intentionally keeps product
 * recommendations off the public website until an affiliate relationship exists.
 */
export const adapterProducts: readonly AdapterProduct[] = [];
